
let initialState = {
    frends: [
        {id: 1, name: 'Dima'},
        {id: 2, name: 'Vlad'},
        {id: 3, name: 'Tania'}
    ]
}

const sitebarReducer = (state = initialState, action) => {



    return state;
}

export default sitebarReducer;